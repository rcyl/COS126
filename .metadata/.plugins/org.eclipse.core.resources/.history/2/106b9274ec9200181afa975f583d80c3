/*
 * main.cpp
 *
 *  Created on: Jul 29, 2018
 *      Author: rcyl
 */

#include <QImage>
#include <iostream>
#include "lfsr.h"

int main() {
	LFSR lfsr("01101000010", 9);
	QImage qImag("src/pipe.png");

	std::cout << "height: " << qImag.height() << std::endl;
	std::cout << "width: " << qImag.width() << std::endl;

	QRgb rgb = qImage.pixel(0, 0);

}
